---
# Jenkins Handlers

- name: Restart jenkins
  ansible.builtin.systemd:
    name: jenkins
    state: restarted
    daemon_reload: true

- name: Reload jenkins
  ansible.builtin.systemd:
    name: jenkins
    state: reloaded
