---
# Docker Role Variables
# =============================================================================

Unexpected scalar token in YAML stream: "enabled"YAML
Unexpected map-value-ind token in YAML stream: ":"YAML
Unexpected scalar token in YAML stream: "true" at line 51, column 18:

# Docker registries to configure
docker_registries: []

# Docker networks to create
docker_networks: []

# Docker volumes to create
docker_volumes: []
