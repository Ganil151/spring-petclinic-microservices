---
# =============================================================================
# Jenkins Setup Playbook
# Deploys Jenkins Master
# =============================================================================

- name: Deploy Jenkins Master
  hosts: jenkins_masters
  become: true
  roles:
    - terraform
    - terragrunt
    - infracost
    - jenkins
